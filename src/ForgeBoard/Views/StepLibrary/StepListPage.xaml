<Page
  x:Class="ForgeBoard.Views.StepLibrary.StepListPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="{StaticResource BgBrush}"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="24,24,24,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock
        Grid.Column="0"
        Text="Step Library"
        FontSize="24"
        FontWeight="Bold"
        Foreground="{StaticResource TextBrush}"
        VerticalAlignment="Center"
      />
      <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
        <Button Content="Import" Click="ImportSteps_Click" />
        <Button Content="Export All" Click="ExportAllSteps_Click" />
        <Button
          Style="{StaticResource FilledAccentButton}"
          Content="New Step"
          Click="NewStep_Click"
        />
      </StackPanel>
    </Grid>

    <ListView
      Grid.Row="1"
      ItemsSource="{Binding Steps}"
      Margin="24,16,24,24"
      SelectionMode="Single"
      SelectedItem="{Binding SelectedStep, Mode=TwoWay}"
    >
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <StackPanel Grid.Column="0" Spacing="2">
                <TextBlock
                  Text="{Binding Name}"
                  FontSize="14"
                  FontWeight="SemiBold"
                  Foreground="{StaticResource TextBrush}"
                />
                <TextBlock
                  Text="{Binding Description}"
                  FontSize="11"
                  Foreground="{StaticResource MutedBrush}"
                  MaxLines="1"
                  TextTrimming="CharacterEllipsis"
                />
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <Border Background="{StaticResource AccentBrush}" CornerRadius="3" Padding="6,2">
                    <TextBlock Text="{Binding StepType}" FontSize="10" Foreground="White" />
                  </Border>
                </StackPanel>
              </StackPanel>

              <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="4"
                VerticalAlignment="Center"
              >
                <Button
                  Content="Export"
                  Click="ExportStep_Click"
                  Tag="{Binding Id}"
                  FontSize="11"
                />
                <Button
                  Content="Duplicate"
                  Click="DuplicateStep_Click"
                  Tag="{Binding Id}"
                  FontSize="11"
                />
                <Button Content="Edit" Click="EditStep_Click" Tag="{Binding Id}" />
                <Button
                  Style="{StaticResource DangerButtonStyle}"
                  Content="Delete"
                  Click="DeleteStep_Click"
                  Tag="{Binding Id}"
                />
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
