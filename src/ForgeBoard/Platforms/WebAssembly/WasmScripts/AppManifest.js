var UnoAppManifest = {
    displayName: "ForgeBoard"
}

// ForgeBoard API configuration
// Empty string = same origin (standalone mode). Set to "http://host:5050" for separate API.
window.__forgeboard_api_url = "http://localhost:5050";
